# Active browser cloaking probe (large optional browser download)
playwright==1.62.0
# Reportability agent (docs/REPORTABILITY_AGENT_DESIGN.md) — opt-in, used only
# by `python app.py assess-reportability`, never by `run`. Version verified
# against the current SDK (messages.parse/count_tokens output_format support
# confirmed on this exact release) before pinning.
anthropic==1.5.0
